# server

Backend game and evaluation logic.

- `core/` game lifecycle engine
- `socket/` Socket.io handlers + input sanitation
- `services/` round orchestration and score scaling
- `storage/` state snapshot persistence
- `evaluator/` Round 4 scoring system
